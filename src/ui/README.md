# /ui

React views + shared components (spec §8): roster manager, single-target planner,
multi-target/hub planner, forward calculator, reverse lookup, settings. Phase 0 is
functional and **unstyled** — visual design is session 0.D (CLAUDE.md invariant 5).
Session 0.5.
