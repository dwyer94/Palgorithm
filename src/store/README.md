# /store

`localStorage`-backed persistence (spec §4.5): `roster`, `savedPlans`, `settings`
(allowed-catch policy, perk sets, server-config presets for the 1.0 contingency), and
`selectedPlayerIds` (which connected server players currently feed the planner).
Session 0.5.
