# /test

Ruleset-vs-oracle correctness tests (spec §11) + solver unit tests. The ruleset
forward/reverse harness is the divergence detector when the 1.0 ruleset lands — keep it
green (CLAUDE.md, Testing).
